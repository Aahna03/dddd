/**
 * Topic: Advanced Linked Lists
 *
 * Q8. Merge two sorted lists
 *    - Beginner: merge values into array then rebuild list.
 *    - Optimized: two-pointer merge directly on list nodes.
 *
 * Q9. Intersection of two linked lists
 *    - Beginner: nested loops to compare nodes.
 *    - Optimized: pointer switching or length difference method.
 *
 * Q10. Remove duplicates from sorted linked list
 *    - Beginner: manually skip duplicates.
 *    - Optimized: one-pass pointer technique.
 */
public class AdvancedLinkedListProblems {

    // Q8: Merge two sorted lists (optimized two-pointer)
    public static ListNode mergeTwoLists(ListNode l1, ListNode l2) {
        // TODO: dummy head + tail pointer merging nodes in order
        return null;
    }

    // Q9: Intersection of two linked lists (length difference)
    public static ListNode getIntersectionNode(ListNode headA, ListNode headB) {
        // TODO: compute lengths or use pointer switching
        return null;
    }

    // Q10: Remove duplicates from sorted linked list
    public static ListNode removeDuplicates(ListNode head) {
        // TODO: iterate and skip nodes with same value
        return head;
    }
}


